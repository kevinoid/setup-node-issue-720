A reproduction for <https://github.com/npm/cmd-shim/issues/71> (where a file
named `node.js` is run instead of `node.exe` in `%PATH%` on Windows) and test
of <https://github.com/npm/cmd-shim/pull/64>.
